{
  "title": "Entity Metadata",
  "config": {
    "step": {
      "user": {
        "title": "Entity Metadata Setup",
        "description": "Creates the integration entry."
      }
    },
    "abort": {
      "already_configured": "This integration is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Entity Metadata Options",
        "data": {
          "max_backups": "Maximum backup files to retain",
          "enable_on_boot": "Import entity metadata from override.yaml file on Home Assistant startup",
          "export_now": "Export entity metadata to overrides.yaml file now."
        }
      }
    }
  }
}
